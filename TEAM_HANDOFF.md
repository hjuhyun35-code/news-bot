# 인스타 다계정 운영 규칙

**이 문서는 사용자가 정한 운영 규칙입니다.** 다른 채팅창에서 인스타 계정 작업을 할 때
이 문서를 먼저 읽고 그 범위 안에서만 작업하세요.

인스타 **공용 인프라는 `news-bot` 창에서 총괄**합니다. 각 계정 창은 자기 계정의
콘텐츠와 저장소만 담당합니다.

작성일 2026-08-02.

---

## 1. 역할 분담

| | 담당 | 하는 일 |
|---|---|---|
| **공용 인프라** | `news-bot` 창 | Meta 앱 설정, 발행 스크립트 원본, 토큰 갱신, 텔레그램 승인, 카드 렌더링 방식, 파이프라인 설계 |
| **계정별 콘텐츠** | 각 계정 창 | 소재 발굴, 문구·캡션 작성, 카드 디자인, 계정 운영, 댓글 대응 |

계정 목록 (2026-08-02 기준):

| 계정 | 저장소 | 담당 창 | 상태 |
|---|---|---|---|
| `@theglassnegative` (아카이브 역사) | `news-bot` | 이 창 | 첫 게시물 발행됨 |
| `singan.theater` (책 요약) | `book-bot` | 별도 창 | 진행 중 |
| 영어권 한국어 학습 | `korean-en` | 미개설 | 계획 |
| 일본향 한국어 학습 | `korean-jp` | 미개설 | 보류 |

---

## 2. 건드리면 안 되는 것

**Meta 개발자 앱 `glassnegative-bot`의 설정.**
모든 계정이 이 앱 하나를 공유합니다. 권한·이용 사례·콜백 URL을 바꾸면 **전 계정이 영향받습니다.**
계정을 테스터로 추가하는 것은 해도 됩니다(아래 4번). 그 외 설정 변경은 `news-bot` 창에 요청하세요.

**`news-bot` 저장소.** 이 창 전용입니다.

**다른 계정의 저장소.** 자기 저장소만 건드리세요.

**이미 발급된 토큰의 재발급.** 재발급하면 다른 창에서 돌던 작업이 조용히 멈춥니다.
토큰이 노출됐거나 만료된 경우에만 재발급하고, 했으면 사용자에게 반드시 알리세요.

**Secret 이름 규칙.** 임의로 바꾸지 마세요. 아래 5번 참조.

---

## 3. 각 계정 창이 할 일

- 자기 저장소 안에서 **자유롭게** 작업
- 계정 세팅(프로 전환, 테스터 추가, 초대 수락, 토큰 발급)은 **해도 됨**
- 콘텐츠 전부 — 소재, 문구, 디자인, 캡션, 대체텍스트
- 발행 스크립트는 `news-bot`의 것을 **복사해서** 자기 저장소에 넣고, 계정명·환경변수명만 수정

**인프라 변경이 필요하면** 사용자에게 "이건 `news-bot` 창에서 처리해야 한다"고 알리고,
무엇을 왜 바꿔야 하는지 한 줄로 정리해주세요.

---

## 4. 새 계정을 붙이는 절차

Meta 앱 세팅은 이미 끝나 있습니다. **다시 만들지 마세요.** 계정만 추가하면 됩니다.

1. 인스타 계정을 **크리에이터**로 전환 (개인 계정은 API 불가, **공개**여야 함)
2. `developers.facebook.com` → 앱 `glassnegative-bot` →
   **앱 역할 → 역할 → 더 보기 → Instagram 테스터** → 계정 아이디 추가
3. 그 계정으로 `https://www.instagram.com/accounts/manage_access/` →
   **테스터 초대 → 수락**  (초대는 알림으로 오지 않음. 이 주소로 직접 가야 함)
4. 앱 → **Instagram 로그인이 포함된 API → 2. 액세스 토큰 생성 → 계정 추가**
   → **해당 계정으로 로그인** (다른 계정으로 로그인하면 거기 글이 올라감)
5. 토큰을 자기 저장소의 GitHub Secrets에 저장

쓰레드도 같은 구조. **Threads 테스터**로 추가하고, 수락은
쓰레드 설정 → **웹사이트 권한** → 앱 및 웹사이트 → 테스터 초대.
(인스타는 "앱 및 웹사이트", 쓰레드는 "웹사이트 권한" — 메뉴 이름이 다름)

토큰 확인:
`https://graph.instagram.com/me?fields=id,username&access_token=토큰`

---

## 5. 표준 규격 (지킬 것)

**저장소 구조**
```
posts/<슬러그>/
    card1.png ... cardN.png     1080x1350
    post.json
scripts/ig_publish.py
.github/workflows/ig-publish.yml
```

**`post.json`**
```json
{
  "slug": "...",
  "caption": "첫 줄에 실제 검색어. 해시태그 5개 이하",
  "cards": [{ "file": "card1.png", "alt": "100자 이내" }]
}
```

**Secret 이름** — 저장소마다 독립이므로 아래 이름을 그대로 쓰세요.
```
IG_ACCESS_TOKEN
IG_USER_ID
THREADS_ACCESS_TOKEN
TELEGRAM_BOT_TOKEN
TELEGRAM_CHAT_ID
ANTHROPIC_API_KEY
```

**카드 만드는 법** — Canva 말고 HTML→PNG.
```
msedge.exe --headless=new --disable-gpu --hide-scrollbars
  --window-size=1080,1350 --virtual-time-budget=9000
  --screenshot="out.png" "file:///.../card.html?card=1"
```
윈도우에 엣지가 기본 설치돼 있어 추가 설치 불필요. 공짜, 완전 자동, 글자가 정확하고
디자인 변경이 CSS 한 줄. **AI 이미지 생성보다 이 방식이 낫습니다.**
일본어·다국어는 Noto Sans JP 등 OFL 폰트 사용(상업 이용 자유).

**발행 안전장치** — 워크플로 `confirm` 칸에 정확히 `PUBLISH`를 넣어야 실제 발행.
다른 값이면 발행 직전까지만 실행(예행연습). **이 장치를 빼지 마세요.**

---

## 6. 모든 계정 공통 규칙

**보안**
- 토큰·시크릿을 **채팅에 붙여넣지 말 것.** GitHub Secrets에 직접 입력.
- 노출됐으면 즉시 재발급하고 사용자에게 알릴 것.
- `token`, `secret`, `key`가 붙은 것 = 공개 금지 / 숫자 ID, 앱 ID = 공개 무방

**신규 계정 워밍업**
- 만들자마자 하루 여러 개를 규칙적으로 올리면 스팸으로 걸림
- 1주차 이틀에 한 번 → 2주차 매일 1개 → 이후 2 → 4 → 6개로 증량
- 시간대도 조금씩 다르게 (정확히 같은 시각 반복은 기계처럼 보임)
- **친구 초대 금지** — 관심 없는 팔로워는 초기 참여율을 떨어뜨려 오히려 해로움

**노출**
- 해시태그 **5개 이하**. 30개 도배는 손해
- **캡션 첫 줄에 실제 검색어**를 넣을 것 (구글이 인스타 공개 게시물을 색인함)
- **대체텍스트는 매번 채울 것.** 인스타 검색이 읽는데 대부분 안 채움 — 공짜 점수
- 캐러셀 피드가 기본, 릴스는 신규 유입용

**콘텐츠 정직성**
- **기록에 없는 것을 쓰지 말 것.** AI는 빈칸을 그럴듯하게 채우므로 명시적 검사 필요
- 저작권: 남의 사진을 출처만 밝히고 쓰는 건 허락받은 게 아님
- AI 이미지를 쓸 거면 정직하게 표기. 사진인 척 금지
- 자동 댓글 답글 금지 — 티가 나고, 오답이 나오면 계정 신뢰가 무너짐

---

## 7. 이미 검증된 것 (다시 조사하지 말 것)

- **Instagram API with Instagram Login** 방식 사용 중. **페이스북 페이지 불필요**
  (페이스북 *계정*은 개발자 사이트 로그인용으로만 필요)
- 필요 권한: `instagram_business_basic`, `instagram_business_content_publish`,
  `instagram_business_manage_comments`, `instagram_business_manage_messages`
- 앱은 **게시(Publish) 안 함, 심사 안 받음.** 내 계정에만 올리므로 불필요
- 캐러셀 6장 발행 + 캡션 + 해시태그 + 대체텍스트 자동 입력 — **실제 발행으로 검증 완료**
- 저장소는 **Public**이어야 함 (인스타가 카드 이미지를 가져가야 하므로)

---

## 8. 알려진 함정

- **GitHub `Create new file`은 현재 보고 있는 폴더 기준.** 하위 폴더에서 전체 경로를 치면
  중복 경로가 생김. **반드시 저장소 첫 화면(root)에서 만들 것**
- **`계정 추가` 전에 테스터 등록 + 수락이 안 되어 있으면** 「개발자 역할 권한 부족」 오류
- **BotFather 등 유명 봇은 가짜가 많음.** 이름은 베낄 수 있으니 **아이디(@)와 파란 인증 배지**로 확인
- **대체텍스트 100자 초과 시** 컨테이너 생성이 거부될 수 있음
- **인스타 ID가 두 종류.** Meta 화면의 번호와 `graph.instagram.com/me`가 주는 번호가 다름. 둘 다 정상
- **텔레그램 봇은 내가 먼저 말을 걸어야** 봇이 나에게 메시지를 보낼 수 있음
- **텔레그램 봇 토큰은 계정마다 따로 만들 것. 절대 공유 금지.**
  같은 토큰으로 두 프로그램이 답을 받아가면(`getUpdates`), **먼저 가져간 쪽이
  가져가고 다른 쪽은 영영 못 받는다.** 텔레그램은 한 번 전달한 소식을 다시 주지
  않는다. 2026-08-04에 실제로 이 일이 있었다 — 승인 단추를 여러 번 눌렀는데
  아무 일도 안 일어났고, 누른 기록은 다른 계정 창의 봇이 가져갔다.
  증상이 조용해서 찾는 데 오래 걸린다. 봇은 BotFather에서 계정마다 새로 만들 것.
- **깃허브 예약 실행(cron)은 정각·30분을 피할 것.** 전 세계 작업이 몰려서
  미뤄지거나 통째로 건너뛴다. `*/30` 으로 걸어둔 워크플로가 19시간 동안 한 번도
  안 걸린 적이 있다. `13,43 * * * *` 처럼 어중간한 분으로 쓸 것

---

## 9. 미해결 (news-bot 창에서 처리 중)

- **토큰 60일 만료 → 자동 갱신 미구현.** 방치하면 두 달 뒤 전 계정이 조용히 멈춤.
  **2026년 10월 초까지 반드시 처리.** 각 창에서 따로 만들지 말 것
- 쓰레드 발행 스크립트 미작성 (토큰만 발급된 상태)
- 파이프라인(수집 → 사실확인 → 카드생성 → 승인 → 발행) 미구축

이 셋이 완성되면 각 계정 저장소에 배포합니다.
