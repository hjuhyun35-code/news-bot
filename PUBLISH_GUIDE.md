# 인스타 게시물 올리는 법 — 작업 지침

카드 이미지가 준비된 뒤, 실제로 인스타에 올리기까지의 순서입니다.
**계정마다 저장소가 다르므로 아래 `<저장소>` 자리를 자기 저장소 이름으로 바꿔 쓰세요.**

> 예: `hjuhyun35-code/news-bot`

---

## 안내할 때 지켜야 할 것 (AI에게)

사용자는 여러 프로젝트를 동시에 진행하므로 **화면 위치를 기억하지 못합니다.**

- **메뉴 경로 대신 전체 주소를 주세요.** "Actions 탭 → 워크플로 선택"이 아니라
  `https://github.com/<저장소>/actions/workflows/ig-publish.yml`
- **클릭 단계는 한 번에 하나씩.** 여러 단계를 한꺼번에 주면 앞질러 가서
  엉뚱한 폴더에 파일이 들어갑니다
- **어느 버튼이 마지막인지 명시하세요.** `Commit changes...` 를 누른 뒤
  팝업에서 한 번 더 눌러야 저장됩니다
- **상태는 직접 확인하세요.** 저장소가 공개면 로그인 없이 확인됩니다:
  - 파일 존재: `https://raw.githubusercontent.com/<저장소>/main/<경로>`
  - 실행 결과: `https://api.github.com/repos/<저장소>/actions/runs?per_page=3`
  - 사용자에게 화면 사진을 요청하기 전에 이걸 먼저 해보세요

---

## 0-A. 카드가 아직 없다면 — 자동으로 만들기

소재 이름만 있으면 사진 수집부터 카드 그리기까지 자동입니다. 두 번 돌립니다.

**1) 사진 모으기**
```
https://github.com/<저장소>/actions/workflows/prepare.yml
```
`Run workflow` → 슬러그 칸을 **비워두면** `queue.json` 의 다음 소재를
알아서 집습니다. 특정 소재를 원하면 슬러그를 적습니다.

저작권 만료 사진만 통과시킵니다. 사진이 부족하면 여기서 멈추고 이유를
알려줍니다. 그럴 땐 `queue.json` 의 그 소재에 `"hold": true` 를 넣고
다음으로 넘어가세요.

**2) 대본 쓰고 카드 그리기**
```
https://github.com/<저장소>/actions/workflows/draft.yml
```
슬러그를 적고 `Run workflow`. 결과는 `posts/<슬러그>/` 에 저장되고
실행 페이지 아래 `draft-<슬러그>` 로도 내려받을 수 있습니다.

**반드시 `check.json` 을 보세요.** 사실 검증 결과입니다. `supported` 가
아닌 항목이 있으면 그 문장은 자료에 없는 내용입니다. 지우거나 고친 뒤
올리세요. 이건 자동으로 안 고쳐집니다 — 사실 오류는 사람이 판단할 몫입니다.

소재를 추가하려면 `queue.json` 에 세 줄만 넣으면 됩니다:
`slug`(폴더 이름), `subject`(한 줄 설명), `wikipedia`(영문 문서 제목).

---

## 0. 준비물

- 카드 이미지 `card1.png` ~ `cardN.png` — **1080 × 1350**, 최대 10장
- `post.json` — 캡션과 카드별 대체텍스트
- 저장소에 `scripts/ig_publish.py` 와 `.github/workflows/ig-publish.yml` 이 있을 것
- 금고(Secrets)에 `IG_ACCESS_TOKEN` 이 있을 것

로컬 폴더 구조:
```
posts/<슬러그>/
    card1.png
    card2.png
    ...
    post.json
```

`<슬러그>`는 영문 소문자로 짧게 (`tunguska`, `voynich` 처럼).

---

## 1. `post.json` 작성

```json
{
  "slug": "voynich",
  "caption": "첫 줄에 사람들이 실제로 검색하는 문장.\n\n본문...\n\n#태그 #다섯개 #이하",
  "cards": [
    { "file": "card1.png", "alt": "100자 이내 영문 설명" },
    { "file": "card2.png", "alt": "..." }
  ]
}
```

지킬 것:

- **캡션 첫 줄 = 검색어.** 구글이 인스타 공개 게시물을 색인합니다
- **해시태그 5개 이하.** 6개 이상은 인스타가 거부합니다
- **대체텍스트는 100자 이내.** 넘으면 컨테이너 생성이 거부될 수 있습니다
- 대체텍스트를 비우지 마세요. 인스타 검색이 읽는데 대부분 안 채웁니다
- 줄바꿈은 `\n` 으로 씁니다. 실제 줄바꿈을 넣으면 JSON이 깨집니다
- 캡션은 2,200자 제한

작성 후 JSON이 유효한지 확인하세요. 깨져 있으면 실행이 실패합니다.

---

## 2. 파일 올리기

**반드시 이 주소에서 시작하세요:**
```
https://github.com/<저장소>/upload/main
```

이 주소가 **저장소 루트의 업로드 화면**입니다.
(`Add file` 메뉴를 찾아 들어가면 현재 폴더 기준이 되어 경로가 어긋납니다)

1. 탐색기에서 **`posts` 폴더를 통째로** 끌어다 놓으세요
   - 파일을 하나씩 끌면 경로가 안 붙습니다
   - 기존 게시물 폴더는 그대로 유지됩니다
2. 목록에 `posts/<슬러그>/card1.png` 처럼 **경로가 붙어 보이는지 확인**
3. 아래로 스크롤 → 초록 **`Commit changes`**

**확인:** 브라우저에서 아래 주소가 이미지로 열리면 성공입니다.
```
https://raw.githubusercontent.com/<저장소>/main/posts/<슬러그>/card1.png
```
열리지 않으면 인스타도 못 가져갑니다.

---

## 3. 예행연습 (발행 안 함)

```
https://github.com/<저장소>/actions/workflows/ig-publish.yml
```

1. 오른쪽 **`Run workflow ▾`**
2. 입력칸 두 개:
   - `slug` → 슬러그 (`voynich`)
   - `confirm` → **`rehearsal`** 그대로 두기
3. 초록 **`Run workflow`**

카드를 인스타에 하나씩 보내 준비시키고, 하나로 묶은 뒤 **발행 직전에 멈춥니다.**
계정에는 아무것도 올라가지 않습니다. 1~2분 걸립니다.

**여기가 통과하면 발행도 확실히 됩니다.**

---

## 4. 실제 발행

같은 주소에서 다시:

- `slug` → 같은 슬러그
- `confirm` → **`PUBLISH`** (전부 대문자, 앞뒤 공백 없이)

`PUBLISH` 철자가 하나라도 다르면 또 예행연습으로 돕니다. 안전장치입니다.

---

## 5. 확인

실행 성공만으로는 **실제 발행인지 예행연습인지 구분되지 않습니다.**
둘 다 성공으로 끝나기 때문입니다.

→ **사용자에게 인스타 앱에서 프로필을 확인해달라고 하세요.** 그게 확실합니다.
(로그아웃 상태의 웹 프로필은 새 계정 게시물을 안 보여줄 때가 있어 근거가 못 됩니다)

---

## 6. 실패했을 때

| 로그에 나오는 것 | 원인과 조치 |
|---|---|
| `IG_ACCESS_TOKEN 이 비어 있습니다` | 금고에 없거나 이름 철자가 다름 |
| `토큰이 거부되었습니다` | 토큰 만료 또는 복사할 때 공백이 섞임 |
| `연결된 계정이 @... 입니다` | 토큰이 다른 계정에 붙어 있음. 재발급 필요 |
| `컨테이너를 만들지 못했습니다` + 권한 관련 | `instagram_business_content_publish` 권한 없음 |
| `컨테이너를 만들지 못했습니다` + URL/fetch 관련 | 저장소가 비공개거나 이미지 경로가 틀림 |
| `제한 시간 안에 준비되지 않았습니다` | 이미지가 너무 크거나 인스타가 느림. 다시 실행 |

스크립트는 대체텍스트가 거부되면 **자동으로 그것만 빼고 재시도**합니다.
로그에 "대체텍스트가 거부되어 없이 재시도합니다"가 보이면 그 카드는
대체텍스트 없이 올라간 것입니다.

---

## 7. 올린 뒤

- **댓글은 손으로 답하세요.** 자동 답글 금지 — 티가 나고, 오답이 나오면
  계정 신뢰가 무너집니다. 특히 올린 지 1시간 안의 답글이 노출에 유리합니다
- **하루 하나면 충분합니다.** 같은 날 두 개를 올리면 서로 노출을 나눠 가져
  어느 쪽이 먹혔는지 알 수 없게 됩니다
- 시간대를 조금씩 바꾸세요. 매일 정확히 같은 시각은 기계처럼 보입니다
- 신규 계정은 2주쯤 사람처럼 쓰다가 하루 2 → 4 → 6개로 늘리세요

---

## 8. 건드리면 안 되는 것

- **Meta 개발자 앱 설정** — 모든 계정이 공유합니다. 하나 바꾸면 전 계정 영향
- **다른 계정의 저장소**
- **`confirm` 안전장치** — 워크플로에서 제거하지 마세요
- **토큰을 채팅에 붙여넣기** — 노출되면 즉시 재발급하고 사용자에게 알릴 것

자세한 배경은 같은 저장소의 `TEAM_HANDOFF.md` 참고.
